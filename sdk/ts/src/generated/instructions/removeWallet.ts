/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/kit';
import { ABL_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const REMOVE_WALLET_DISCRIMINATOR = 3;

export function getRemoveWalletDiscriminatorBytes() {
  return getU8Encoder().encode(REMOVE_WALLET_DISCRIMINATOR);
}

export type RemoveWalletInstruction<
  TProgram extends string = typeof ABL_PROGRAM_ADDRESS,
  TAccountAuthority extends string | IAccountMeta<string> = string,
  TAccountListConfig extends string | IAccountMeta<string> = string,
  TAccountWalletEntry extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            IAccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountListConfig extends string
        ? WritableAccount<TAccountListConfig>
        : TAccountListConfig,
      TAccountWalletEntry extends string
        ? WritableAccount<TAccountWalletEntry>
        : TAccountWalletEntry,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveWalletInstructionData = { discriminator: number };

export type RemoveWalletInstructionDataArgs = {};

export function getRemoveWalletInstructionDataEncoder(): Encoder<RemoveWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: REMOVE_WALLET_DISCRIMINATOR })
  );
}

export function getRemoveWalletInstructionDataDecoder(): Decoder<RemoveWalletInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getRemoveWalletInstructionDataCodec(): Codec<
  RemoveWalletInstructionDataArgs,
  RemoveWalletInstructionData
> {
  return combineCodec(
    getRemoveWalletInstructionDataEncoder(),
    getRemoveWalletInstructionDataDecoder()
  );
}

export type RemoveWalletInput<
  TAccountAuthority extends string = string,
  TAccountListConfig extends string = string,
  TAccountWalletEntry extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  listConfig: Address<TAccountListConfig>;
  walletEntry: Address<TAccountWalletEntry>;
};

export function getRemoveWalletInstruction<
  TAccountAuthority extends string,
  TAccountListConfig extends string,
  TAccountWalletEntry extends string,
  TProgramAddress extends Address = typeof ABL_PROGRAM_ADDRESS,
>(
  input: RemoveWalletInput<
    TAccountAuthority,
    TAccountListConfig,
    TAccountWalletEntry
  >,
  config?: { programAddress?: TProgramAddress }
): RemoveWalletInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountListConfig,
  TAccountWalletEntry
> {
  // Program address.
  const programAddress = config?.programAddress ?? ABL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    listConfig: { value: input.listConfig ?? null, isWritable: true },
    walletEntry: { value: input.walletEntry ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.listConfig),
      getAccountMeta(accounts.walletEntry),
    ],
    programAddress,
    data: getRemoveWalletInstructionDataEncoder().encode({}),
  } as RemoveWalletInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountListConfig,
    TAccountWalletEntry
  >;

  return instruction;
}

export type ParsedRemoveWalletInstruction<
  TProgram extends string = typeof ABL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    listConfig: TAccountMetas[1];
    walletEntry: TAccountMetas[2];
  };
  data: RemoveWalletInstructionData;
};

export function parseRemoveWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRemoveWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      listConfig: getNextAccount(),
      walletEntry: getNextAccount(),
    },
    data: getRemoveWalletInstructionDataDecoder().decode(instruction.data),
  };
}
